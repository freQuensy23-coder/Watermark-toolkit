{
  "name": "watermark-toolkit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-assets": "node scripts/prepare-assets.mjs",
    "write-build-meta": "node scripts/write-build-meta.mjs",
    "test": "npm run prepare-assets && npm run write-build-meta && node tests/test.mjs",
    "dev": "npm run prepare-assets && npm run write-build-meta && wrangler pages dev public",
    "deploy": "npm run prepare-assets && npm run write-build-meta && wrangler pages deploy public --project-name watermark-toolkit --branch main"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
