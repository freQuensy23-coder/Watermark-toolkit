{
  "name": "watermark-toolkit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-assets": "node scripts/prepare-assets.mjs",
    "test": "npm run prepare-assets && node tests/test.mjs",
    "dev": "npm run prepare-assets && wrangler pages dev public",
    "deploy": "npm run prepare-assets && wrangler pages deploy public --project-name watermark-toolkit --branch main"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
